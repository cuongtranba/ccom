# Admin key for the server — unlocks full project/node management in the admin UI.
# Without this, the admin UI is read-only and nodes self-register freely.
ADMIN_KEY=change-me-to-a-secret

# PostgreSQL password (used by both the postgres container and the server)
POSTGRES_PASSWORD=inv
